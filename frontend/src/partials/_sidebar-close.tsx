import React from 'react';

const SidebarClose = () => {
    return (
        <button type="button" className="sidebar-close">
            <span>
                Close Sidebar
            </span>
        </button>
    );
};

export default SidebarClose;
